<resources>

  <style name="Theme.dwebbrowser" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Primary brand color. -->
    <item name="colorPrimary">@color/colorPrimary_dark</item>
    <item name="colorPrimaryVariant">@color/colorPrimaryVariant_dark</item>
    <item name="colorOnPrimary">@color/colorOnPrimary_dark</item>
    <!-- Secondary brand color. -->
    <item name="colorSecondary">@color/colorSecondary_dark</item>
    <item name="colorSecondaryVariant">@color/colorSecondaryVariant_dark</item>
    <item name="colorOnSecondary">@color/colorOnSecondary_dark</item>
    <!-- Status bar color. -->
    <item name="android:statusBarColor">@color/colorPrimary_dark</item>
    <!-- Customize your theme here. -->
    <item name="android:textAllCaps">false</item>
    <!-- 关闭窗口预览，避免跳转activity时白屏问题 -->
    <item name="android:windowDisablePreview">true</item>
    <item name="isLightTheme">false</item>
  </style>

  <style name="Theme.DwebBrowser.Transparent" parent="Theme.Material3.Dark.NoActionBar">
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:background">@android:color/transparent</item>
    <item name="android:windowFrame">@null</item>
    <item name="android:title">null</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="android:backgroundDimEnabled">false</item>
    <item name="android:windowIsFloating">true</item>
  </style>

  <style name="Theme.RustExample" parent="android:Theme.Material.NoActionBar">
    <item name="android:statusBarColor">@color/purple_700</item>
  </style>

  <!-- Splash启动页Style -->
  <style name="Theme.App.Starting" parent="Theme.SplashScreen">
    <!--启动画面背景颜色-->
    <item name="windowSplashScreenBackground">#000000</item>
    <!-- 启动画面icon图标：这里可以是图片、帧动画等-->
    <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
    <item name="windowSplashScreenIconBackgroundColor">#FF00FF</item>

    <!-- icon动画在关闭之前显示的时长：最长时间为1000毫秒-->
    <item name="windowSplashScreenAnimationDuration">200</item>

    <!-- 启动画面底部的 Brand 图片-->
    <!--<item name="android:windowSplashScreenBrandingImage">@drawable/ic_branding</item>-->

    <!-- Splash退出后的主题-->
    <item name="postSplashScreenTheme">@style/Theme.AppCompat.DayNight.NoActionBar</item>
  </style>
</resources>
